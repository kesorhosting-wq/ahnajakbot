import { NextResponse } from 'next/server';import { supabaseAdmin } from '@/lib/supabase';import { env } from '@/lib/env';
export async function POST(req:Request){const auth=req.headers.get('x-internal-secret'); if(auth!==env.backendSecret)return NextResponse.json({error:'Forbidden'},{status:403}); const b=await req.json(); await supabaseAdmin.from('guild_admins').upsert({guild_id:b.guild_id,guild_name:b.guild_name,discord_user_id:b.admin_user_id,role:'setup_admin'},{onConflict:'guild_id,discord_user_id'}); await supabaseAdmin.from('settings').upsert({guild_id:b.guild_id,admin_role_id:b.admin_role_id||null,description:'Welcome to ᴀʜɴᴀᴊᴀᴋᴍᴄ ꜱᴛᴏʀᴇ',how_to_use:'Click a category, choose a product, enter your Minecraft username, and pay with KHQR.',terms_privacy:'No refund after successful delivery unless staff approves.',merchant_name:'AhnajakMCStore',merchant_id:'sonmeng_leng@bkrt',webhook_secret:env.defaultWebhookSecret},{onConflict:'guild_id'}); return NextResponse.json({ok:true})}
